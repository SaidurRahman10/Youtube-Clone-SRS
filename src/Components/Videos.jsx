import { Box } from '@mui/system';
import React from 'react';

const Videos = ({videos}) => {
    console.log(videos);
    return (
        <div>
            <Box sx={{color:'white'}}>

            vIDEOS
            </Box>
        </div>
    );
};

export default Videos;